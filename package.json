{
  "name": "contract-shield-cli",
  "version": "0.1.0",
  "description": "A CLI tool that verifies preconditions, postconditions, and invariants in contracts without altering their original source code. `contracts-shield-cli` enforces Design by Contract principles externally, ensuring contracts behave as expected while preserving their integrity.",
  "main": "index.js",
  "scripts": {
    "test": "jest"
  },
  "author": "Ion Gireada",
  "license": "MIT",
  "dependencies": {
    "ansi-colors": "^4.1.3",
    "commander": "^14.0.0"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
